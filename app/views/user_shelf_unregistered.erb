  <h1 class="bookshelf-title">Welcome to User # <%= @user.gr_id %>'s Bookshelf</h1>

  <div class="bookshelf-wrapper">
    <div class="bookshelf" data-gr-id="<%= @user.gr_id %>">
      <% @first_20_book_objects.each do |book| %>
        <%= erb :_book, locals: {book: book} %>
      <% end %>
    </div>
  </div>